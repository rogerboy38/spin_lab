[project]
name = "spin_lab"
authors = [{ name = "rogerboy38" }]
description = "Educational slot machine simulator & next-token probability research lab built on Frappe"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
